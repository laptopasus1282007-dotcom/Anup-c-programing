// strrev() :- its convert string is reverse string.
#include<stdio.h>
void main()
{
    char st [20];
    printf("Enter string : ");
    gets(st); //anup
    printf("string : %s\n",st);
    strrev(st);
    printf("Reverse String : %s\n",st); //puna
}